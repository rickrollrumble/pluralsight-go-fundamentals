package main

import "fmt"

func main() {
	// lists all environment variables on system as key-value pairs
	// fmt.Println(os.Environ())
	// writes out all environment variables, one on each line
	// for _, env := range os.Environ() {
	// 	fmt.Println(env)
	// }
	fmt.Println("We're in env.go")
	variables_main()
}
